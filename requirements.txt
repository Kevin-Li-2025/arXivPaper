requests>=2.28.0
feedparser>=6.0.0
schedule>=1.2.0
python-dotenv>=1.0.0
click>=8.0.0
pydantic>=2.5.0
jinja2>=3.1.0
beautifulsoup4>=4.12.0
python-dateutil>=2.8.0
Flask>=2.3.0

# Optional dependencies for advanced scoring methods
# For transformer-based scoring
sentence-transformers>=2.2.0  # For general transformer scoring
transformers>=4.21.0  # For SciBERT support (optional)
torch>=1.12.0  # For SciBERT support (optional)
numpy>=1.21.0  # Required by transformers

# For API-based scoring
openai>=1.0.0  # For OpenAI GPT models
anthropic>=0.7.0  # For Claude models
google-generativeai>=0.3.0  # For Gemini models

# Note: DeepSeek and xAI use OpenAI-compatible APIs, so only openai package is needed 